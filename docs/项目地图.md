# 玄枢项目地图

## 📍 模块定位速查表

### 核心层

| 文件 | 定位 | 职责 | 关键方法 |
|------|------|------|----------|
| `agent.py` | 核心入口 | UniversalAgent主类 | `_process_simple()`, `run()` |
| `launcher.py` | 启动器 | 环境检测、模型选择、启动引导 | `run()`, `setup_ollama()` |
| `web_app.py` | Web服务 | FastAPI服务、REST API | 所有 `@app.` 端点 |
| `config.py` | 配置中心 | 全局配置常量 | 所有 `_*` 配置项 |

### 数据管理层

| 文件 | 定位 | 职责 | 关键方法 |
|------|------|------|----------|
| `model_providers.py` | 模型管理 | 配置、切换、调用模型 | `call_model()`, `config_manager` |
| `conversation_manager.py` | 对话管理 | 对话历史CRUD | `add_user_message()`, `save_current()` |
| `memory_core.py` | 核心记忆 | SQLite持久化记忆 | `save()`, `load()` |
| `memory_enhanced.py` | 增强记忆 | 向量搜索(可选) | - |

### 进化引擎层 (`evolution/`)

| 文件 | 定位 | 职责 |
|------|------|------|
| `multi_agent.py` | 多Agent协作 | 多个Agent协同工作 |
| `planner.py` | 任务规划 | 将复杂任务分解 |
| `react_loop.py` | React循环 | 推理+行动循环 |
| `reflection.py` | 反思机制 | 任务复盘与改进 |
| `role_switcher.py` | 角色切换 | 不同角色切换 |
| `skill_generator.py` | 技能生成 | 自动生成新技能 |
| `skill_validator.py` | 技能验证 | 验证技能有效性 |
| `trajectory.py` | 轨迹记录 | 执行轨迹存储 |
| `workflow.py` | 工作流定义 | 工作流DSL |
| `workflow_engine.py` | 工作流引擎 | 执行工作流 |

### 集群子模块 (`evolution/cluster/`)

| 文件 | 定位 | 职责 | 关键类 |
|------|------|------|--------|
| `capability.py` | 能力评估 | 评估节点能力分 | `CapabilityAssessor` |
| `cluster_api.py` | 集群API | REST API扩展 | - |
| `connection.py` | 连接管理 | TCP连接、节点通信 | `ClusterNode`, `ClusterManager`, `ClusterServer`, `ClusterClient` |
| `discovery.py` | 节点发现 | 局域网节点发现 | `ClusterDiscovery` |
| `manager.py` | 简化管理器 | 独立房间管理 | `ClusterManager` |
| `protocol.py` | 通信协议 | 消息序列化 | `ClusterMessage` |
| `room_api.py` | 房间API | 房间管理REST | - |
| `scheduler.py` | 任务调度 | 智能任务分配 | `TaskScheduler` |
| `security.py` | 集群安全 | 安全相关 | - |

### 技能系统 (`skills/`)

| 文件 | 定位 | 功能 |
|------|------|------|
| `base.py` | 技能基类 | Skill基类定义 |
| `code/python_exec/` | 代码执行 | 安全执行Python代码 |
| `io/file_list/` | 文件列表 | 列出目录文件 |
| `io/file_read/` | 文件读取 | 读取文件内容 |
| `io/file_write/` | 文件写入 | 写入文件 |
| `network/web_fetch/` | 网页抓取 | 获取网页内容 |
| `audit/project_security_audit/` | 安全审计 | 项目安全检查 |

### 安全层 (`security/`)

| 文件 | 定位 | 职责 |
|------|------|------|
| `firewall/approval.py` | 审批防火墙 | 高风险操作审批 |
| `sandbox/docker_sandbox.py` | Docker沙箱 | 隔离代码执行 |

### Web层 (`web/static/`)

| 文件 | 定位 | 职责 |
|------|------|------|
| `index.html` | 主页面 | 单页应用 |
| `css/style.css` | 样式 | 界面样式 |

---

## 🔗 模块依赖图

```
                    ┌─────────────────────────────────────────┐
                    │              launcher.py                 │
                    │  (启动入口，用户交互)                     │
                    └───────────────────┬─────────────────────┘
                                        │
                                        │ 选择模型、启动服务
                                        ▼
┌────────────────────────────────────────────────────────────────┐
│                         web_app.py                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ /api/chat │  │/api/rooms│  │/api/models│ │/api/approvals│ │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └──────┬───────┘ │
└────────┼─────────────┼─────────────┼───────────────┼─────────┘
         │             │             │               │
         ▼             ▼             ▼               ▼
┌────────────────────────────────────────────────────────────────┐
│                       agent.py                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ UniversalAgent: _process_simple(), skills_registry      │  │
│  └─────────────────────────────────────────────────────────┘  │
│            │                    │                    │        │
└────────────┼────────────────────┼────────────────────┼────────┘
             │                    │                    │
             ▼                    ▼                    ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│model_providers.py│  │conversation_mgr.py│  │   skills/        │
│                  │  │                  │  │  ┌────────────┐  │
│ call_model()     │  │ add_message()    │  │  │ base.py   │  │
│ config_manager   │  │ save_current()   │  │  │ code/      │  │
│                  │  │                  │  │  │ io/        │  │
└──────────────────┘  └──────────────────┘  │  │ network/   │  │
                                              │  │ audit/     │  │
                                              │  └────────────┘  │
                                              └──────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│                     evolution/                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │react_loop│  │ planner  │  │reflection│  │  cluster/    │  │
│  │          │  │          │  │          │  │              │  │
│  │ _think() │  │plan()    │  │reflect() │  │manager.py    │  │
│  │ _act()   │  │          │  │          │  │connection.py │  │
│  └──────────┘  └──────────┘  └──────────┘  │scheduler.py  │  │
│                                             │capability.py │  │
│                                             └──────────────┘  │
└────────────────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│                      security/                                  │
│  ┌──────────────────┐  ┌──────────────────────────────────┐    │
│  │firewall/approval │  │sandbox/docker_sandbox.py         │    │
│  │                  │  │                                  │    │
│  │ApprovalStore     │  │DockerSandbox.execute()           │    │
│  └──────────────────┘  └──────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────┘
```

---

## 📦 跨文件调用速查

### Agent调用链
```
agent.py::UniversalAgent._process_simple()
    ├── model_providers.py::call_model()
    ├── skills_registry (动态加载)
    │   └── skills/code/python_exec/python_exec.py
    └── memory_core.py (如启用)
```

### Web请求处理链
```
web_app.py::api_chat()
    ├── conversation_manager.py::add_user_message()
    ├── agent.py::get_agent()._process_simple()
    └── conversation_manager.py::add_assistant_message()
```

### 房间创建链
```
web_app.py::create_room()
    └── ensure_cluster_initialized()
        └── ClusterManager.create_room()
```

### 集群节点加入链
```
web_app.py::join_room()
    └── ClusterClient.join()
        └── ClusterServer._handle_client()
            └── ClusterManager.add_node()
```

---

## 🎯 关键数据结构

### ModelConfig (model_providers.py)
```python
@dataclass
class ModelConfig:
    provider: ProviderType  # ollama/openai_compatible/custom
    name: str              # 配置名称
    model_name: str        # 模型名称
    api_base: str          # API地址
    api_key: str = ""      # API密钥
```

### ClusterNode (evolution/cluster/connection.py)
```python
class ClusterNode:
    node_id: str
    ip: str
    model: str
    role: str              # manager/worker
    mode: str              # auto/manual
    status: str            # online/offline
    capability_score: float # 能力评分 0.0-1.0
    connection: socket      # TCP连接
    pending_tasks: list    # 待处理任务
```

### Skill (skills/base.py)
```python
class Skill(ABC):
    name: str
    description: str
    parameters: list
    async def execute(**kwargs) -> dict
```

---

## 🔍 文件搜索关键词

当需要定位某个功能时，按关键词搜索：

| 功能 | 搜索关键词 | 目标文件 |
|------|-----------|----------|
| 模型调用 | `call_model`, `call_ollama` | model_providers.py |
| 对话保存 | `add_user_message`, `save_current` | conversation_manager.py |
| 房间创建 | `create_room`, `room_id` | web_app.py, connection.py |
| 任务调度 | `assign_task`, `TaskScheduler` | scheduler.py, connection.py |
| 技能执行 | `execute_skill`, `skills_registry` | agent.py, base.py |
| 审批流程 | `ApprovalStore`, `set_decision` | web_app.py, approval.py |
| Docker执行 | `DockerSandbox`, `execute` | docker_sandbox.py |
| 能力评估 | `CapabilityAssessor`, `evaluate` | capability.py |
| WebSocket | `WebSocket`, `ws_connections` | connection.py, index.html |
| 记忆存储 | `save_memory`, `get_all_core_memory` | memory_core.py |

---

*最后更新: 2026-05-08*
