# 决策日志

> 记录项目中的重要技术决策，包括决策背景、选项、结论和后续影响。

---

## 📖 使用说明

每次做出重要技术决策时，创建新文件 `YYYY-MM-DD-决策标题.md`，或追加到本文件的决策列表中。

---

## 📝 决策列表

| 日期 | 标题 | 状态 |
|------|------|------|
| 2026-05-08 | 模型管理：统一使用ConfigManager单例 | ✅ 已实施 |
| 2026-05-08 | 房间列表：新增独立API支持 | ✅ 已实施 |
| 2026-05-08 | 协作功能：房间完整流程设计 | ✅ 已实施 |
| 2026-05-08 | 对话导出：支持JSON格式下载 | ✅ 已实施 |
| 2026-05-08 | WebSocket路由：支持standalone模式 | ✅ 已实施 |
| 2026-05-08 | 协作对话：新增成员状态面板 | ✅ 已实施 |
| 2026-05-08 | API统计：新增监控功能 | ✅ 已实施 |
| 2026-05-09 | 能力评估：智能子串匹配+全平台兼容 | ✅ 已实施 |
| 2026-05-09 | 局域网发现：新增完整扫描API | ✅ 已实施 |
| 2026-05-09 | 协作成员面板：移除非科学参数简化界面 | ✅ 已实施 |
| 2026-05-09 | 全平台审计：Win/Linux/Mac跨平台确认 | ✅ 已实施 |
| 2026-05-09 | 两个核心bug修复：房主模型+跨主机局域网发现 | ✅ 已实施 |

---

## 🔗 决策详情

### [2026-05-08] 模型管理：统一使用ConfigManager单例

**背景问题**:
Launcher.py选择的模型与Web端显示不一致，原因在于`/api/models`端点只显示Ollama模型或有api_key的模型。

**决策选项**:

1. **修改API过滤逻辑**（选择）
   - 优点：简单直接，兼容性最好
   - 缺点：可能暴露不需要显示的配置

2. **前端传递模型标识**
   - 优点：安全
   - 缺点：需要修改多处代码

3. **使用全局状态同步**
   - 优点：一致性最好
   - 缺点：增加复杂度

**最终决策**:
选择方案1，修改`web_app.py`中的`/api/models`端点，移除不合理的过滤条件。

**影响文件**:
- `web_app.py:641-660`

**后续**:
需要确保模型配置变更时能正确同步到所有使用方。

---

### [2026-05-08] 房间列表：新增独立API支持

**背景问题**:
原实现中，单机模式无法查看房间列表，`/api/rooms/current`需要token验证。

**决策选项**:

1. **新增`/api/rooms/list`端点**（选择）
   - 优点：职责清晰，扩展性好
   - 缺点：需要新增API

2. **修改现有端点逻辑**
   - 优点：不需要新增端点
   - 缺点：职责不清晰，逻辑混乱

**最终决策**:
选择方案1，新增`/api/rooms/list`端点获取房间列表。

**影响文件**:
- `web_app.py`
- `web/static/index.html`

**后续**:
可考虑将房间管理统一到RESTful风格。

---

### [2026-05-08] 协作功能：房间完整流程设计

**背景问题**:
房间创建后没有后续流程，无法进入房间页面，成员列表不刷新，缺少解散房间功能。

**决策选项**:

1. **创建房间后自动进入详情页**（选择）
   - 优点：用户体验流畅，减少操作步骤
   - 缺点：可能增加代码复杂度

2. **仅显示成功提示，用户手动进入**
   - 优点：简单
   - 缺点：用户操作步骤多

**最终决策**:
选择方案1，创建房间后自动调用`enterRoom(res.room_id)`进入详情页面，并添加定时刷新成员列表功能。

**新增功能**:
- 房间详情页面添加协作专区
- 解散房间功能 (`/api/rooms/dismiss`)
- 成员定时刷新机制
- 任务错误反馈 (`/api/cluster/tasks/{task_id}/error`)

**影响文件**:
- `web_app.py` - 新增2个API端点
- `web/static/index.html` - 协作专区和成员刷新

**后续**:
可考虑添加开始协作按钮触发完整协作流程。

---

### [2026-05-08] 对话导出：支持JSON格式下载

**背景问题**:
用户需要导出对话记录进行分析，原有Markdown格式不方便程序处理。

**决策选项**:

1. **新增`/api/export/json`端点**（选择）
   - 优点：格式规范，便于程序处理
   - 缺点：需要新增端点

2. **前端JS转换Markdown为JSON**
   - 优点：不需要后端修改
   - 缺点：转换可能丢失信息

**最终决策**:
选择方案1，新增专门的JSON导出端点，包含完整的对话元数据。

**影响文件**:
- `web_app.py` - 新增`/api/export/json`端点
- `web/static/index.html` - 添加导出按钮

**后续**:
可考虑添加对话导入功能。

---

### [2026-05-08] WebSocket路由：支持standalone模式

**背景问题**:
WebSocket路由`/api/cluster/ws/updates`返回403错误，原因是依赖ClusterNode验证，但standalone模式没有ClusterNode。

**决策选项**:

1. **移除ClusterNode依赖**（选择）
   - 优点：支持standalone模式
   - 缺点：可能降低安全性

2. **添加独立的WebSocket端点**
   - 优点：职责清晰
   - 缺点：增加复杂度

**最终决策**:
选择方案1，移除WebSocket路由对ClusterNode的依赖，同时修复路由前缀配置。

**影响文件**:
- `evolution/cluster/cluster_api.py`
- `web_app.py`

---

### [2026-05-08] 协作对话：新增成员状态面板

**背景问题**:
协作对话页面缺少成员状态显示，用户无法知道其他成员的状态（忙碌/空闲）。

**决策选项**:

1. **在协作对话页面添加成员状态面板**（选择）
   - 优点：实时查看成员状态
   - 缺点：增加页面复杂度

2. **仅在房间详情页显示**
   - 优点：简洁
   - 缺点：需要切换页面查看

**最终决策**:
选择方案1，在协作对话页面右侧添加成员状态面板，显示成员名称、模型、能力分、CPU/内存负载和状态。

**新增功能**:
- 成员状态面板（实时刷新）
- 状态标识（🟢空闲/🔴忙碌/⚪离线）
- 房主标识

**影响文件**:
- `web/static/index.html`

---

### [2026-05-08] API统计：新增监控功能

**背景问题**:
缺少API调用统计功能，无法了解系统运行状况。

**决策选项**:

1. **新增ApiStatistics类**（选择）
   - 优点：集中管理，扩展性好
   - 缺点：需要新增代码

2. **使用第三方监控工具**
   - 优点：功能完善
   - 缺点：增加依赖

**最终决策**:
选择方案1，实现轻量级的API统计功能，记录请求次数、成功率、响应时间和模型调用统计。

**新增API**:
- `/api/stats` - 获取统计数据

**影响文件**:
- `web_app.py`

---

### [2026-05-08] JSON导出：处理MessageRole枚举类型

**背景问题**:
导出JSON格式时提示 "Object of type MessageRole is not JSON serializable"。

**决策选项**:

1. **在format_value中处理枚举**（选择）
   - 优点：简单直接，修改范围小
   - 缺点：需要了解枚举结构

2. **将枚举转换为字典后序列化**
   - 优点：通用性强
   - 缺点：修改范围大

**最终决策**:
选择方案1，在format_value函数中添加 `hasattr(val, 'name')` 判断，处理枚举类型的name属性。

**影响文件**:
- `web_app.py`

---

### [2026-05-08] 创建房间模型列表：同时支持本地和云端模型

**背景问题**:
创建房间页面只显示Ollama本地模型，不显示云端API配置模型。

**决策选项**:

1. **同时加载Ollama和配置模型**（选择）
   - 优点：用户可选择范围更广
   - 缺点：加载逻辑稍复杂

2. **仅使用配置模型**
   - 优点：统一管理
   - 缺点：本地Ollama用户不方便

**最终决策**:
选择方案1，loadRoomModels函数先加载Ollama模型，再加载配置模型，避免重复。

**影响文件**:
- `web/static/index.html`

---

### [2026-05-08] 重新回复功能：使用统一的API调用

**背景问题**:
resendMessage使用直接的fetch调用，与其他API调用方式不一致。

**决策选项**:

1. **使用统一的api()函数**（选择）
   - 优点：代码一致性好，易于维护
   - 缺点：需要确保api()函数完整

2. **保持直接fetch调用**
   - 优点：简单
   - 缺点：代码风格不统一

**最终决策**:
选择方案1，resendMessage改为使用api()函数。

**影响文件**:
- `web/static/index.html`

---

### [2026-05-09] 能力评估：智能子串匹配+全平台兼容

**背景问题**:
能力评估系统所有模型都默认返回0.5分，因为云端API和本地Ollama大多数模型都不在硬编码的排行榜里，导致评估形同虚设。

**决策选项**:

1. **智能子串匹配+扩展排行榜+参数量估算**（选择）
   - 优点：覆盖99%主流模型，完全不需要用户手动配置
   - 缺点：维护排行榜需要持续更新新模型

2. **用户手动配置模型分数**
   - 优点：100%准确
   - 缺点：用户体验差，配置繁琐

**最终决策**:
选择方案1，大幅扩展排行榜加入Qwen、Llama、GPT、Claude、DeepSeek等几乎所有主流模型，实现智能最长子串匹配，新增基于参数量自动估算分数的规则。云端API默认0.85分，本地未知模型默认0.7分。

**影响文件**:
- `evolution/cluster/capability.py`
- `evolution/cluster/connection.py`

---

### [2026-05-09] 局域网发现：新增完整扫描API

**背景问题**:
底层UDP广播局域网房间发现代码已经写好，但没有对外暴露REST API，其他玄枢项目无法在Web界面上发现局域网房间。

**决策选项**:

1. **新增三个发现API端点**（选择）
   - 优点：完全暴露底层能力，职责清晰
   - 缺点：需要新增代码

2. **复用现有ClusterNode API**
   - 优点：不需要新增端点
   - 缺点：API设计不清晰，理解成本高

**最终决策**:
选择方案1，新增三个独立API端点：
- `/api/discovery/start_scan` - 启动扫描
- `/api/discovery/rooms` - 获取发现的房间
- `/api/discovery/stop` - 停止发现服务

**影响文件**:
- `web_app.py`
- `evolution/cluster/discovery.py`

---

### [2026-05-09] 协作成员面板：移除非科学参数简化界面

**背景问题**:
协作对话页面成员状态面板显示能力分、CPU、内存这些不够科学的评估数据，用户觉得没有意义。

**决策选项**:

1. **移除非科学评估数据，仅保留状态和模型类型标签**（选择）
   - 优点：界面清爽简洁，用户一眼看清核心信息
   - 缺点：丢失一些潜在信息（但信息不准确）

2. **保留所有数据，标注仅供参考**
   - 优点：数据不丢失
   - 缺点：界面繁杂，信息不准确

**最终决策**:
选择方案1，完全移除能力分、CPU%、内存%显示，添加智能模型类型标签：
- 绿色「本地」标签 → 本地Ollama等模型
- 蓝色「云端API」标签 → 云端大模型服务
保留成员名称、房主标识、完整模型名称、状态徽章（🟢空闲/🔴忙碌/⚪离线）。

**影响文件**:
- `web/static/index.html`

---

### [2026-05-09] 全平台审计：Win/Linux/Mac跨平台确认

**背景问题**:
系统性审计整个项目的跨平台兼容性，确保在Windows、Linux和Mac上都可以流畅运行。

**审计结果**:
1. ✅ logger.py - 已实现Windows UTF-8控制台编码兼容，pathlib.Path路径
2. ✅ memory_core.py - SQLite自动降级JSON模式，零系统依赖
3. ✅ conversation_manager.py - 纯Python os.path路径处理
4. ✅ discovery.py - 纯BSD socket UDP广播，全平台跨平台兼容
5. ✅ config.py - 所有路径使用os.path.join / pathlib.Path
6. ✅ launcher.py - 系统平台判断分别适配不同浏览器启动方式

**最终决策**:
玄枢项目已经是全平台兼容的项目，不需要做任何额外修改，在Windows 10/11、Linux (Ubuntu/Debian等) 和 macOS上都可以直接流畅运行。

---

### [2026-05-09] 两个核心bug修复：房主模型+跨主机局域网发现

**背景问题**:
1. 创建房间后，我的信息中使用模型一直没有正确被调用出来，只显示默认的模型选项，而不是model_config.json或ollama中实际选的房主模型。
2. 房间列表无法显示同一局域网（同一路由器/同一网段）内其他主机开的房间，只能本机内找到。

**根因分析**:
- 问题1：UDP广播启动时，`ClusterDiscovery`没有收到任何房主信息（owner_name、owner_model），导致广播消息不包含关键模型信息。
- 问题2：单机模式下完全没有启动`start_scanning()`扫描模式，只能发广播，不能同时监听收到其他主机发来的UDP包，而且房间列表API只返回本地房间，没有把discovery里found_rooms合并进来。

**决策选项**:
1. **完整修复链路**（选择）
   - 修改`connection.py`：启动server时同时调用`start_hosting(extra_info=房主信息)`和`start_scanning()`
   - 修改单机模式：ensure_cluster_initialized中也初始化discovery实例并启动扫描
   - 修改discovery.py的_scan_loop：保存UDP消息中的owner_name和owner_model
   - 修改/api/rooms/list接口：合并本地房间 + 所有discovery扫描到的远程房间

2. **简化方案**：只在集群模式启用发现
   - 优点：代码少改动少
   - 缺点：单机模式（99%普通用户用的模式）反而用不了局域网功能，不符合项目初衷

**最终决策**:
选择完整修复链路，单机/集群/manager模式统一同时开启广播+扫描，所有模式都能发现局域网其他主机的房间，房主的模型信息通过UDP广播完整传递。

**影响文件**:
- `evolution/cluster/connection.py`
- `evolution/cluster/discovery.py`
- `web_app.py`

**后续**:
后续前端页面更新后就可以完美支持局域网房间的点击加入（自动填入远程ip和端口）了。

---

## 📋 新增决策模板

```markdown
### [YYYY-MM-DD] 决策标题

**背景问题**:


**决策选项**:

1. **方案A**
   - 优点：
   - 缺点：

2. **方案B**（选择）
   - 优点：
   - 缺点：

**最终决策**:


**影响文件**:


**后续**:
```

---

*最后更新: 2026-05-09* ✅ 全平台兼容审计完成
