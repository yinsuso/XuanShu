# ==============================
# 玄枢项目 - Python 依赖清单
# ==============================
# 【重要提示】关于 Python 版本兼容性：
# - 推荐使用 Python 3.10 ~ 3.12（PyTorch 生态完全兼容）
# - Python 3.13/3.14 可以运行核心功能，但 PyTorch 相关重型依赖可能遇到 DLL 初始化问题
# - 本项目已自动将 PyTorch 相关依赖设为可选，跳过不影响核心功能
# ==============================

# ========= 必选依赖（核心功能必须） =========
requests>=2.31.0
duckduckgo-search>=4.4.0
fastapi>=0.109.0
uvicorn>=0.27.0
jinja2>=3.1.2
tenacity>=8.2.3
colorama>=0.4.6
beautifulsoup4>=4.12.0
lxml>=5.0.0
python-multipart>=0.0.9

# ========= 可选增强依赖 =========
# 注意：以下包依赖 PyTorch，在 Python 3.14 Windows 环境下可能出现 WinError 1114 DLL 初始化失败
# 如遇报错，可以忽略！项目核心功能（Ollama/云端API调用、技能系统、多机协作）完全不受影响
# 如需完整安装推荐降级到 Python 3.10 或 3.12：
chromadb>=0.5.0
sentence-transformers>=2.5.0

# WebSocket 支持 (FastAPI 集群协作实时通信)
websockets>=12.0

# 安全增强 (security hardening)
docker>=7.1.0
